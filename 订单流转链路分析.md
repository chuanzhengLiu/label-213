# 票务系统订单流转链路分析

## 一、订单状态定义

| 状态值 | 含义 | 说明 |
|--------|------|------|
| 1 | 已下单 | 游客提交订单后的初始状态，库存已扣减但尚未核销 |
| 2 | 已核销 | 管理员确认游客已使用门票，订单完成 |
| 3 | 已取消 | 管理员取消订单，库存已回补 |

> 来源：[TicketOrder.java](scenic-manager-backend/src/main/java/com/scenic/manager/entity/TicketOrder.java) `orderStatus` 字段注释

---

## 二、订单完整生命周期

```
游客下单                     管理员核销                  管理员取消
  │                           │                          │
  ▼                           ▼                          ▼
[已下单(1)] ──────────→ [已核销(2)]            [已取消(3)] ──→ 物理删除
     │                                                │
     │  ←── 仅状态1可流转到2或3 ──→                    │
     │                                                │
     │  库存已扣减                               库存已回补
     │  统计不计入                               统计不计入
     │                                                │
     ▼                                                ▼
  不可再改状态                                   仅此状态可删除
```

---

## 三、各操作详细分析

### 3.1 创建订单（游客下单）

**入口**：`POST /api/order/create`

**代码位置**：[TicketOrderServiceImpl.java:create()](scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java)

**执行步骤**：

1. 根据 `ticketTypeId` 查询票种信息
2. 校验库存：`ticketType.stock >= dto.ticketNum`，不足则抛出异常
3. **乐观锁扣减库存**：
   - 条件：`id = ticketTypeId AND version = 当前version`
   - 更新：`stock = stock - ticketNum`，`version = version + 1`
   - 若更新行数为 0，说明并发冲突，抛出"库存扣减失败，请重试"
4. 生成订单编号（`OrderNoUtil.generateOrderNo()`）
5. 计算总金额：`totalAmount = price × ticketNum`
6. 插入订单记录，`orderStatus = 1`（已下单）

**对库存的影响**：✅ **扣减** `ticketNum` 张

**对统计的影响**：❌ **不计入**（统计只算 status=2）

**前端交互**（[BuyTicket.vue](scenic-manager-frontend/src/views/visitor/BuyTicket.vue)）：
- 游客选择景区 → 加载该景区下票种列表（含实时库存）
- 选择票种 → 显示价格、库存、有效期
- 库存为 0 的票种显示"已售罄"，不可点击
- 购买数量上限 = 当前库存
- 下单成功后刷新票种列表，库存实时更新

---

### 3.2 核销订单（状态 1 → 2）

**入口**：`PUT /api/order/updateStatus?id=xxx&orderStatus=2`

**代码位置**：[TicketOrderServiceImpl.java:updateStatus()](scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java)

**前置校验**：
- 订单必须存在
- 订单当前状态必须为 1（已下单），否则抛出"仅已下单状态的订单可修改状态"
- 目标状态必须为 2 或 3

**执行步骤**：
1. 校验通过后，将 `orderStatus` 设为 2
2. 更新 `updateTime`
3. 保存到数据库

**对库存的影响**：❌ **不变**（库存已在下单时扣减，核销不再操作库存）

**对统计的影响**：✅ **开始计入**（status=2 的订单才会被统计）

**前端交互**（[OrderManagement.vue](scenic-manager-frontend/src/views/admin/OrderManagement.vue)）：
- 仅 `orderStatus === 1` 的行显示"核销"按钮
- 点击后弹出确认框"是否核销该订单？"
- 成功后刷新列表

---

### 3.3 取消订单（状态 1 → 3）

**入口**：`PUT /api/order/updateStatus?id=xxx&orderStatus=3`

**代码位置**：[TicketOrderServiceImpl.java:updateStatus()](scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java)

**前置校验**：
- 同核销，订单必须存在且状态为 1

**执行步骤**：
1. **回补库存**：查询对应票种，执行 `stock = stock + ticketNum`
2. 将 `orderStatus` 设为 3
3. 更新 `updateTime`
4. 保存到数据库

**对库存的影响**：✅ **回补** `ticketNum` 张

**对统计的影响**：❌ **不计入**（status=3 不在统计范围）

**前端交互**（[OrderManagement.vue](scenic-manager-frontend/src/views/admin/OrderManagement.vue)）：
- 仅 `orderStatus === 1` 的行显示"取消"按钮
- 点击后弹出确认框"是否取消该订单？"
- 成功后刷新列表

---

### 3.4 删除订单（物理删除）

**入口**：`DELETE /api/order/delete/{id}`

**代码位置**：[TicketOrderServiceImpl.java:delete()](scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java)

**前置校验**：
- 订单必须存在
- **仅允许删除状态为 3（已取消）的订单**，否则抛出"仅允许删除已取消状态的订单"

**执行步骤**：
1. 校验通过后，执行 `ticketOrderMapper.deleteById(id)` 物理删除

**对库存的影响**：❌ **不变**（库存已在取消时回补，删除不再操作库存）

**对统计的影响**：❌ **不影响**（已取消的订单本就不在统计范围）

**前端交互**（[OrderManagement.vue](scenic-manager-frontend/src/views/admin/OrderManagement.vue)）：
- 仅 `orderStatus === 3` 的行显示"删除"按钮
- 点击后弹出确认框"是否删除该订单？"
- 成功后刷新列表

---

### 3.5 编辑订单（修改购票人信息）

**入口**：`PUT /api/order/update`

**代码位置**：[TicketOrderServiceImpl.java:update()](scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java)

**允许修改的字段**：仅 `userName` 和 `userPhone`

**对库存的影响**：❌ **不变**

**对统计的影响**：❌ **不影响**

---

## 四、统计逻辑详解

**入口**：`GET /api/order/statistics`

**代码位置**：[TicketOrderServiceImpl.java:statistics()](scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java)

**核心过滤条件**：`orderStatus = 2`（仅统计已核销的订单）

**统计指标**：

| 指标 | 计算方式 | 数据来源 |
|------|----------|----------|
| 总销售额 | `SUM(totalAmount)` WHERE status=2 | 所有已核销订单的金额之和 |
| 总销量 | `SUM(ticketNum)` WHERE status=2 | 所有已核销订单的票数之和 |
| 平均单价 | `totalSales / totalNum` | 前端计算，后端不返回 |
| 票种销量排行 | `GROUP BY ticketTypeId`，取 TOP 10 | 按票种分组统计销量，降序排列 |

**前端展示**（[Statistics.vue](scenic-manager-frontend/src/views/admin/Statistics.vue)）：
- 支持按时间范围筛选
- 三张统计卡片：总销售额、总销量、平均单价
- 票种销量排行表格

---

## 五、库存与统计的影响矩阵

| 操作 | 库存变化 | 计入统计 | 页面展示变化 |
|------|----------|----------|-------------|
| 创建订单（→状态1） | ✅ 扣减 ticketNum | ❌ | 游客端：库存数字减少；管理端：订单列表新增一条"已下单" |
| 核销订单（1→2） | ❌ 不变 | ✅ 开始计入 | 管理端：订单状态变为"已核销"；统计页数据增加 |
| 取消订单（1→3） | ✅ 回补 ticketNum | ❌ | 游客端：库存数字恢复；管理端：订单状态变为"已取消" |
| 删除订单（删状态3） | ❌ 不变 | ❌ | 管理端：该订单从列表消失 |
| 编辑订单 | ❌ 不变 | ❌ | 管理端：购票人信息更新 |

---

## 六、关键设计特征与潜在问题

### 6.1 "先扣库存"模型

系统采用**下单即扣库存**的策略，而非"核销时才扣库存"。这意味着：

- **已下单但未核销的订单会占用库存**，其他游客看到的库存是扣减后的值
- 如果大量订单停留在"已下单"状态不核销也不取消，库存会被持续占用，导致"有票但卖不出去"

### 6.2 统计只算已核销

统计模块只计算 `orderStatus = 2` 的订单，这意味着：

- **已下单（状态1）的订单金额不计入销售额和销量**——虽然库存已被扣减
- 存在"库存减少 ≠ 销量增加"的现象：下单后库存立即减少，但直到核销前，统计页的销量和销售额不会变化
- 如果需要查看"含未核销的预估销售额"，当前接口无法满足

### 6.3 状态流转的单向性

```
状态1 → 状态2（不可逆）
状态1 → 状态3（不可逆）
```

- 已核销的订单无法取消或回退
- 已取消的订单无法恢复为已下单
- 这保证了库存一致性：一旦核销，库存不会回补；一旦取消，库存已回补且不可再核销

### 6.4 删除是物理删除

删除操作直接从数据库移除记录（`deleteById`），而非软删除。这意味着：

- 已取消的订单一旦删除，数据不可恢复
- 删除不影响库存（因为取消时已回补）和统计（因为取消的订单本就不计入）

### 6.5 取消订单的库存回补无乐观锁

对比创建订单时使用乐观锁扣减库存，取消订单时的库存回补操作**没有使用乐观锁**：

```java
// 创建时：有乐观锁
updateWrapper.eq(TicketType::getVersion, ticketType.getVersion())

// 取消时：无乐观锁
updateWrapper.eq(TicketType::getId, order.getTicketTypeId())
```

在高并发场景下，如果同时有管理员取消订单和游客下单操作同一票种，可能出现库存数据不一致。

---

## 七、完整数据流图

```
┌─────────────────────────────────────────────────────────────────┐
│                         游客端（BuyTicket.vue）                   │
│                                                                 │
│  选择景区 → 加载票种(含库存) → 选择票种 → 填写信息 → 提交下单     │
│       │                                        │                │
│       │                                        ▼                │
│       │                              POST /api/order/create     │
│       │                                        │                │
│       │                                        ▼                │
│       └────────────── 刷新票种列表(库存减少) ◀────┘                │
└─────────────────────────────────────────────────────────────────┘

                              │
                              ▼

┌─────────────────────────────────────────────────────────────────┐
│                      后端（TicketOrderServiceImpl）               │
│                                                                 │
│  create():  校验库存 → 乐观锁扣减stock → 插入订单(status=1)      │
│                                                                 │
│  updateStatus(id, 2):  校验status=1 → 更新status=2（核销）       │
│  updateStatus(id, 3):  校验status=1 → 回补stock → 更新status=3  │
│                                                                 │
│  delete(id):  校验status=3 → 物理删除                            │
│  update(id):  仅修改userName/userPhone                           │
│                                                                 │
│  statistics():  查询status=2的订单 → 汇总销售额/销量/排行         │
└─────────────────────────────────────────────────────────────────┘

                              │
                              ▼

┌─────────────────────────────────────────────────────────────────┐
│                      管理端（OrderManagement.vue / Statistics.vue）│
│                                                                 │
│  订单管理页：                                                    │
│    - 查看所有订单（支持按状态/订单号/手机号/时间筛选）              │
│    - 状态1：显示[编辑][核销][取消]按钮                            │
│    - 状态2：显示[编辑]按钮                                       │
│    - 状态3：显示[编辑][删除]按钮                                  │
│                                                                 │
│  统计分析页：                                                    │
│    - 总销售额 = SUM(已核销订单.totalAmount)                      │
│    - 总销量 = SUM(已核销订单.ticketNum)                          │
│    - 平均单价 = 总销售额 / 总销量                                 │
│    - 票种销量排行 TOP 10                                         │
└─────────────────────────────────────────────────────────────────┘
```

---

## 八、核心代码索引

| 模块 | 文件 | 关键方法/行号 |
|------|------|-------------|
| 订单实体 | [TicketOrder.java](scenic-manager-backend/src/main/java/com/scenic/manager/entity/TicketOrder.java) | `orderStatus` 字段 (L59) |
| 票种实体 | [TicketType.java](scenic-manager-backend/src/main/java/com/scenic/manager/entity/TicketType.java) | `stock` 字段 (L42), `version` 乐观锁 (L57) |
| 创建订单 | [TicketOrderServiceImpl.java](scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java) | `create()` (L37-L83) |
| 核销/取消 | [TicketOrderServiceImpl.java](scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java) | `updateStatus()` (L158-L188) |
| 删除订单 | [TicketOrderServiceImpl.java](scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java) | `delete()` (L142-L154) |
| 编辑订单 | [TicketOrderServiceImpl.java](scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java) | `update()` (L122-L138) |
| 统计逻辑 | [TicketOrderServiceImpl.java](scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java) | `statistics()` (L191-L240) |
| 订单控制器 | [TicketOrderController.java](scenic-manager-backend/src/main/java/com/scenic/manager/controller/TicketOrderController.java) | 全部接口 |
| 游客购票页 | [BuyTicket.vue](scenic-manager-frontend/src/views/visitor/BuyTicket.vue) | `handleBuy()` (L237-L270) |
| 订单管理页 | [OrderManagement.vue](scenic-manager-frontend/src/views/admin/OrderManagement.vue) | 操作按钮条件渲染 (L56-L75) |
| 统计分析页 | [Statistics.vue](scenic-manager-frontend/src/views/admin/Statistics.vue) | 数据展示 (L26-L52) |
| 数据库建表 | [init.sql](sql/init.sql) | `ticket_order` 表 (L62-L79), `ticket_type` 表 (L45-L58) |
