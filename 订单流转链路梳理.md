# 景区票务系统订单流转链路梳理

## 一、订单状态定义

| 状态值 | 状态名称 | 说明 |
|--------|----------|------|
| 1 | 已下单 | 初始状态，游客下单后自动进入 |
| 2 | 已核销 | 后台确认验票后，订单完成 |
| 3 | 已取消 | 订单被取消（库存会恢复） |

**实体类定义**：[TicketOrder.java](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-backend/src/main/java/com/scenic/manager/entity/TicketOrder.java#L58-L60)

---

## 二、订单完整生命周期流转图

```
游客下单
    ↓
【创建订单】状态=1（已下单）
    ├─ 扣减库存 ✓
    ├─ 生成订单号
    └─ 记录订单金额、数量
    ↓
后台操作分支：
    ├─→ 【核销订单】状态=2（已核销）
    │       └─ 不改动库存
    │
    ├─→ 【取消订单】状态=3（已取消）
    │       └─ 恢复库存 ✓（加回 ticket_num 张）
    │
    └─→ 【编辑订单】（任意状态均可操作）
            └─ 仅修改购票人姓名/手机号，不影响库存和统计
    ↓
【删除订单】（仅状态=3时允许）
    └─ 物理删除数据库记录，不影响库存（因为取消时已恢复）
```

---

## 三、各操作对各维度的影响对照表

| 操作 | 触发时机 | 库存影响 | 销量/销售额统计 | 页面展示 | 代码位置 |
|------|----------|----------|----------------|----------|----------|
| **创建订单** | 游客购票提交 | **扣减**（stock - ticket_num），使用乐观锁防并发 | ❌ 不计入统计（状态=1） | 列表显示黄色"已下单"标签，库存数字减少 | [TicketOrderServiceImpl.java#L35-L83](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L35-L83) |
| **核销订单** | 状态=1时后台点击"核销" | ❌ 不改动 | ✅ **计入**统计（状态=2） | 列表显示绿色"已核销"标签 | [TicketOrderServiceImpl.java#L156-L188](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L156-L188)（`updateStatus` 完整方法） |
| **取消订单** | 状态=1时后台点击"取消" | **恢复**（stock + ticket_num，库存恢复逻辑见 [L173-L183](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L173-L183)） | ❌ 不计入统计（状态=3） | 列表显示灰色"已取消"标签，库存数字加回 | [TicketOrderServiceImpl.java#L156-L188](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L156-L188)（`updateStatus` 完整方法） |
| **编辑订单** | **任意状态**后台点击"编辑"；后端不校验状态，前端按钮无 v-if 常驻显示 | ❌ 不改动 | ❌ 不影响 | 仅更新姓名/手机号显示 | [TicketOrderServiceImpl.java#L120-L138](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L120-L138)（`update` 方法） |
| **删除订单** | 后台点击"删除"（仅状态=3允许） | ❌ 不改动（取消时已恢复） | ❌ 不影响（状态=3本就不计入） | 记录从列表消失 | [TicketOrderServiceImpl.java#L140-L154](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L140-L154)（`delete` 方法） |

---

## 四、核心操作详细流程

> 核销和取消共用后端同一个方法 `updateStatus()`，完整代码见：[TicketOrderServiceImpl.java#L156-L188](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L156-L188)

### 4.1 创建订单流程（游客端）

**入口**：[BuyTicket.vue](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-frontend/src/views/visitor/BuyTicket.vue) → `handleBuy()`

**后端代码**：[TicketOrderServiceImpl.java#L35-L83](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L35-L83)

后端事务流程（`@Transactional` 保证原子性）：
1.  查询票种信息，校验票种存在
2.  校验库存：`ticketType.stock >= dto.ticketNum`
3.  **乐观锁扣减库存**：
    ```sql
    UPDATE ticket_type 
    SET stock = stock - #{num}, version = version + 1
    WHERE id = #{id} AND version = #{oldVersion}
    ```
    - 若 updateCount = 0 说明并发冲突，抛出异常回滚
4.  生成唯一订单号（OrderNoUtil）
5.  计算总金额：`price × ticketNum`
6.  插入订单记录，`order_status = 1`
7.  前端成功后刷新票种列表，显示最新库存

**关键点**：
- ✅ 下单即扣库存，不是核销才扣
- ✅ 使用乐观锁（version字段）防止超卖
- ✅ 整个操作在一个事务中，任何步骤失败都会回滚库存

### 4.2 核销订单流程（后台）

**入口**：[OrderManagement.vue#L208-L224](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-frontend/src/views/admin/OrderManagement.vue#L208-L224) → `handleVerify()`，调用接口 `updateOrderStatus(id, 2)`

**后端代码**：核销与取消共用同一个方法 [TicketOrderServiceImpl.java#L156-L188](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L156-L188)（`updateStatus` 方法完整范围）

后端流程：
1.  校验订单存在，且当前状态必须为 1（已下单）
2.  校验传入的目标状态只能是 2（已核销）或 3（已取消）
3.  因为目标状态是 2（核销），**跳过库存恢复分支**
4.  更新订单状态为 2，更新 updateTime
5.  ❌ **不改动库存**（下单时已经扣过了）

**关键点**：
- ✅ 核销只是确认交易完成，不涉及库存二次变动
- ✅ **只有状态=2的订单才会计入销量/销售额统计**
- ✅ 前端核销按钮仅在 `row.orderStatus === 1` 时显示（见 [OrderManagement.vue#L57-L62](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-frontend/src/views/admin/OrderManagement.vue#L57-L62)）

### 4.3 取消订单流程（后台）

**入口**：[OrderManagement.vue#L226-L242](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-frontend/src/views/admin/OrderManagement.vue#L226-L242) → `handleCancel()`，调用接口 `updateOrderStatus(id, 3)`

**后端代码**：核销与取消共用同一个方法 [TicketOrderServiceImpl.java#L156-L188](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L156-L188)（`updateStatus` 方法完整范围），其中库存恢复逻辑在 [L173-L183](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L173-L183)

后端流程：
1.  校验订单存在，且当前状态必须为 1（已下单）
2.  校验传入的目标状态只能是 2（已核销）或 3（已取消）
3.  因为目标状态是 3（取消），进入库存恢复分支：
    - 查询对应票种
    - 执行 `stock = stock + order.ticketNum` 把库存加回来
4.  更新订单状态为 3，更新 updateTime

**关键点**：
- ✅ 只有"已下单"状态才能取消
- ✅ 取消后库存立即恢复，这张票可以重新售卖
- ✅ 取消的订单不会出现在统计中
- ✅ 前端取消按钮仅在 `row.orderStatus === 1` 时显示（见 [OrderManagement.vue#L63-L68](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-frontend/src/views/admin/OrderManagement.vue#L63-L68)）

### 4.4 编辑订单流程（后台）

**入口**：[OrderManagement.vue#L56](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-frontend/src/views/admin/OrderManagement.vue#L56) → `<el-button>` 编辑按钮，**无 v-if 条件，所有状态订单常驻显示**；点击后触发 `handleEdit(row)` → `handleSubmit()` 调用 `updateOrder(form)`

**后端代码**：[TicketOrderServiceImpl.java#L120-L138](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L120-L138)（`update` 方法完整范围）

后端流程：
1.  根据 id 查询订单，校验订单存在；**不校验订单状态，任意状态均可编辑**
2.  仅当传入的 `userName != null` 时更新购票人姓名
3.  仅当传入的 `userPhone != null` 时更新购票人手机号
4.  更新 `updateTime`，保存到数据库
5.  ❌ **不改动库存**、**不改变 orderStatus**、**不影响统计**

**关键点**：
- ✅ 后端 `update()` 方法**只校验订单是否存在**，完全没有状态判断逻辑
- ✅ 后端只能修改购票人姓名（userName）和手机号（userPhone）两个字段
- ✅ 前端"编辑"按钮**对所有状态（已下单/已核销/已取消）订单都显示**，没有 `v-if` 状态限制
- ✅ 即使传入 orderStatus / totalAmount / ticketNum 等字段，后端也不会处理（直接忽略）

### 4.5 删除订单流程（后台）

**入口**：[OrderManagement.vue#L244-L260](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-frontend/src/views/admin/OrderManagement.vue#L244-L260) → `handleDelete()`

**后端代码**：[TicketOrderServiceImpl.java#L140-L154](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L140-L154)

后端流程：
1.  根据 id 查询订单，校验订单存在
2.  校验状态必须为 3（已取消）→ 这是硬限制，其他状态删除直接抛异常
3.  物理删除数据库记录
4.  ❌ 不改动库存（因为取消时库存已经恢复过了）

**关键点**：
- ✅ 设计保护：只能删除"已取消"的订单，防止误删有效订单
- ✅ 删除是物理删除，记录从数据库消失
- ✅ 不影响任何统计（因为已取消订单本就不统计）
- ✅ 前端删除按钮仅在 `row.orderStatus === 3` 时显示（见 [OrderManagement.vue#L69-L74](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-frontend/src/views/admin/OrderManagement.vue#L69-L74)）

---

## 五、统计逻辑详解

**入口**：[Statistics.vue](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-frontend/src/views/admin/Statistics.vue) → `loadData()`

后端统计代码：[TicketOrderServiceImpl.java#L191-L240](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L191-L240)

### 统计过滤条件
```java
wrapper.eq(TicketOrder::getOrderStatus, 2);  // 只统计已核销的订单！
```

### 统计指标计算

| 指标 | 计算方式 |
|------|----------|
| **总销售额 totalSales** | 所有已核销订单的 `total_amount` 累加 |
| **总销量 totalNum** | 所有已核销订单的 `ticket_num` 累加 |
| **平均单价** | 前端计算：totalSales / totalNum |
| **票种销量排行** | 按 `ticket_type_id` 分组聚合 `ticket_num`，取前10名排序 |

### 重要结论
- ❌ **已下单（状态=1）但未核销的订单，不计入销量和销售额**
- ❌ **已取消（状态=3）的订单，不计入统计**
- ✅ **只有"已核销"（状态=2）的订单，才算真正完成交易，进入统计**

---

## 六、前端页面展示对应规则

### 订单列表页 [OrderManagement.vue](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-frontend/src/views/admin/OrderManagement.vue#L46-L52)

| 状态 | 标签颜色 | 显示文字 | 可用操作按钮 |
|------|----------|----------|--------------|
| 1（已下单） | warning（黄色） | 已下单 | 编辑、核销、取消 |
| 2（已核销） | success（绿色） | 已核销 | 编辑（姓名/手机号） |
| 3（已取消） | info（灰色） | 已取消 | 编辑、删除 |

> **注意**："编辑"按钮始终显示（无 v-if 条件），核销/取消仅状态=1时显示，删除仅状态=3时显示。
> 前端按钮条件见：[OrderManagement.vue#L56-L74](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-frontend/src/views/admin/OrderManagement.vue#L56-L74)

### 购票页（游客端）[BuyTicket.vue](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-frontend/src/views/visitor/BuyTicket.vue#L53-L56)
- 直接读取 `ticket.stock` 显示剩余库存
- stock = 0 时显示"已售罄"标签，卡片置灰不可点击
- 下单成功后自动重新拉取票种列表，刷新库存数字

### 统计页 [Statistics.vue](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-frontend/src/views/admin/Statistics.vue)
- 数字直接来自后端返回的 `totalSales`、`totalNum`
- 票种排行表格只显示销量最高的前10个票种
- 支持按时间范围筛选

---

## 七、关键设计要点总结

1.  **库存扣减时机**：下单就扣，不是核销才扣，避免超卖
2.  **并发控制**：使用 MyBatis-Plus `@Version` 乐观锁，防止并发下单导致超卖
3.  **状态机约束**：只有 1→2 或 1→3 是合法转换，状态不可逆
4.  **删除保护**：只能删除已取消的订单，防止误操作丢失数据
5.  **统计口径**：**只认已核销**！已下单未核销是"待确认"，不算真正成交
6.  **事务保证**：所有涉及库存+订单双写的操作都加了 `@Transactional`，保证数据一致性

---

## 八、API 接口清单

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/api/order/create` | 创建订单（游客） |
| GET | `/api/order/list` | 分页查询订单列表 |
| PUT | `/api/order/update` | 修改订单（仅购票人信息） |
| PUT | `/api/order/updateStatus?id=&orderStatus=` | 修改订单状态（核销/取消） |
| DELETE | `/api/order/delete/{id}` | 删除订单（仅已取消状态） |
| GET | `/api/order/statistics` | 获取统计数据 |

**控制器代码**：[TicketOrderController.java](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/scenic-manager-backend/src/main/java/com/scenic/manager/controller/TicketOrderController.java)

---

## 九、数据库表关系

```
scenic_spot (景区)
    ↑ 1:N
ticket_type (票种: 含 price, stock, version)
    ↑ 1:N
ticket_order (订单: 含 order_status, ticket_num, total_amount)
```

- `ticket_order.ticket_type_id` → `ticket_type.id`
- 库存字段在 `ticket_type` 表，不在订单表
- 乐观锁通过 `ticket_type.version` 字段实现

**建表脚本**：[init.sql](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-autumn/sql/init.sql#L60-L79)
