# 启动说明

## 快速启动步骤

### 1. 启动MySQL和Redis（使用Docker Compose）

```bash
# 在项目根目录执行
docker-compose up -d

# 等待MySQL初始化完成（约30秒），然后检查容器状态
docker-compose ps

# 查看MySQL日志确认初始化完成
docker-compose logs mysql | grep "ready for connections"
```

### 2. 启动后端

```bash
cd scenic-manager-backend

# 方式一：使用Maven直接运行（推荐开发环境）
mvn spring-boot:run

# 方式二：打包后运行（推荐生产环境）
mvn clean package -DskipTests
java -jar target/scenic-manager-backend-1.0.0.jar
```

后端启动成功后：
- 应用地址: http://localhost:8088
- Swagger文档: http://localhost:8088/swagger-ui/index.html

### 3. 启动前端

```bash
cd scenic-manager-frontend

# 安装依赖（首次运行）
npm install

# 启动开发服务器
npm run dev
```

前端启动成功后：
- 应用地址: http://localhost:3000
- 登录页: http://localhost:3000/login

## 测试流程

### 1. 管理员登录测试

1. 访问 http://localhost:3000/login
2. 使用超级管理员账号登录：
   - 账号: `admin`
   - 密码: `123456`
3. 登录成功后进入管理后台

### 2. 功能测试

#### 用户管理
- 查看用户列表
- 新增用户（测试账号：test/123456）
- 编辑用户信息
- 删除用户（仅超级管理员）

#### 景区管理
- 查看景区列表
- 新增景区
- 编辑景区信息
- 删除景区（需确保无关联票种）

#### 票种管理
- 选择景区后查看票种列表
- 新增票种
- 编辑票种信息
- 查看库存

#### 订单管理
- 查看订单列表
- 编辑订单信息（仅购票人信息）
- 核销订单（状态1→2）
- 取消订单（状态1→3，恢复库存）
- 删除订单（仅已取消状态）

#### 统计分析
- 查看总销售额
- 查看总销量
- 查看票种销量排行

### 3. 游客购票测试

1. 访问 http://localhost:3000/visitor/buy-ticket
2. 选择景区
3. 选择票种
4. 输入购买数量
5. 点击"立即购票"
6. 填写购票人信息
7. 确认购票
8. 查看订单编号

### 4. 订单流程测试

1. 游客端下单（状态：已下单）
2. 管理员登录后台
3. 在订单管理中查看订单
4. 点击"核销"按钮（状态：已核销）
5. 或点击"取消"按钮（状态：已取消，库存恢复）

## 常见问题排查

### 1. MySQL连接失败

**错误**: `Communications link failure`

**解决方案**:
- 检查MySQL容器是否运行: `docker-compose ps`
- 检查MySQL端口是否被占用: `lsof -i:3306`
- 检查`application.yml`中的数据库配置
- 确认数据库密码为`root123456`

### 2. Redis连接失败

**错误**: `Unable to connect to Redis`

**解决方案**:
- 检查Redis容器是否运行: `docker-compose ps`
- 检查Redis端口是否被占用: `lsof -i:6379`
- 检查`application.yml`中的Redis配置

### 3. 前端无法连接后端

**错误**: `Network Error` 或 `CORS error`

**解决方案**:
- 确认后端已启动: 访问 http://localhost:8088/swagger-ui/index.html
- 检查`vite.config.js`中的代理配置
- 检查后端`application.yml`中的跨域配置

### 4. Token失效

**错误**: `401 Unauthorized`

**解决方案**:
- Token过期（2小时），重新登录
- 检查Cookie中是否有token
- 检查Redis中token是否过期

### 5. 权限不足

**错误**: `403 Forbidden`

**解决方案**:
- 确认当前登录用户角色
- 超级管理员（role=1）可执行所有操作
- 普通管理员（role=2）不可执行删除操作

### 6. 库存扣减失败

**错误**: `库存扣减失败，请重试`

**原因**: 乐观锁冲突，并发下单时可能发生

**解决方案**:
- 前端提示用户重试
- 检查票种库存是否充足
- 检查数据库`ticket_type`表的`version`字段

### 7. 数据库字符集问题

**错误**: 中文乱码

**解决方案**:
- 确认数据库字符集为`utf8mb4`
- 确认表字符集为`utf8mb4`
- 检查`application.yml`中的数据库URL是否包含`useUnicode=true&characterEncoding=utf8`

## 数据库初始化

如果使用Docker Compose，数据库会自动初始化。如果手动安装MySQL，需要：

1. 创建数据库：
```sql
CREATE DATABASE scenic_manager DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

2. 执行初始化脚本：
```bash
mysql -uroot -proot123456 scenic_manager < sql/init.sql
```

## 端口说明

- **后端**: 8088
- **前端**: 3000
- **MySQL**: 3306
- **Redis**: 6379

如需修改端口，请修改对应配置文件。

## 开发建议

1. **后端开发**:
   - 使用Swagger测试接口
   - 查看控制台日志排查问题
   - 使用Postman测试复杂接口

2. **前端开发**:
   - 使用浏览器开发者工具查看网络请求
   - 使用Vue DevTools调试
   - 查看控制台错误信息

3. **数据库调试**:
   - 使用MySQL客户端连接数据库
   - 查看表结构和数据
   - 检查索引和约束

## 生产环境部署

1. **后端**:
   - 修改`application.yml`中的数据库和Redis连接
   - 使用`mvn clean package`打包
   - 使用`java -jar`运行或部署到Tomcat

2. **前端**:
   - 使用`npm run build`构建
   - 将`dist`目录部署到Nginx
   - 配置Nginx反向代理到后端

3. **数据库**:
   - 使用生产环境MySQL
   - 配置数据库备份
   - 优化数据库性能

4. **Redis**:
   - 使用生产环境Redis
   - 配置Redis持久化
   - 设置Redis密码
