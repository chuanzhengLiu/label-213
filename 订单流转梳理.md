# 票务订单流转梳理

> 目标：把"游客下单 → 后台核销 / 取消 / 删除 → 统计展示"这条链路上，**库存、销量、销售额、页面展示**这些会被影响的点全部摆清楚，便于排查与后续改动。

代码定位：

- 订单实体：[TicketOrder.java](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-backend/src/main/java/com/scenic/manager/entity/TicketOrder.java)
- 票种实体：[TicketType.java](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-backend/src/main/java/com/scenic/manager/entity/TicketType.java)
- 订单 Controller：[TicketOrderController.java](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-backend/src/main/java/com/scenic/manager/controller/TicketOrderController.java)
- 订单 Service 实现：[TicketOrderServiceImpl.java](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java)
- 票种 Service 实现：[TicketTypeServiceImpl.java](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketTypeServiceImpl.java)
- 游客下单页：[BuyTicket.vue](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-frontend/src/views/visitor/BuyTicket.vue)
- 后台订单管理页：[OrderManagement.vue](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-frontend/src/views/admin/OrderManagement.vue)
- 统计页：[Statistics.vue](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-frontend/src/views/admin/Statistics.vue)

---

## 1. 关键模型字段

### 1.1 `ticket_order`（订单）

| 字段 | 含义 | 备注 |
| --- | --- | --- |
| `orderStatus` | 订单状态 | **1-已下单 / 2-已核销 / 3-已取消** |
| `ticketTypeId` | 关联票种 | 与 `ticket_type` 关联 |
| `ticketNum` | 购票数量 | 影响库存、销量 |
| `totalAmount` | 订单总金额 | 创建时按 `price * ticketNum` 一次性快照写入，不会再因票种调价而变化 |

字段定义见 [TicketOrder.java#L57-L60](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-backend/src/main/java/com/scenic/manager/entity/TicketOrder.java#L57-L60)。

### 1.2 `ticket_type`（票种）

| 字段 | 含义 | 备注 |
| --- | --- | --- |
| `stock` | 库存（剩余可售张数） | 真正影响"还能不能买"的字段 |
| `version` | 乐观锁版本号 | 下单时配合扣减库存使用 |

定义见 [TicketType.java#L40-L58](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-backend/src/main/java/com/scenic/manager/entity/TicketType.java#L40-L58)。

> 注意：系统里**没有"已售数量 / 销量"这种独立字段**。"销量"和"销售额"完全在统计接口里通过订单表实时聚合得到（见第 5 节）。

---

## 2. 订单状态机

```
        创建订单(create)
              │
              ▼
        ┌──────────┐
        │ 1 已下单  │──── 编辑 update（仅改姓名/手机号，不动状态）
        └──┬────┬──┘
   核销 2  │    │  取消 3
           ▼    ▼
   ┌──────────┐  ┌──────────┐
   │ 2 已核销 │  │ 3 已取消  │──── 删除 delete（物理删除，仅在 3 状态下允许）
   └──────────┘  └──────────┘
```

合法迁移规则集中在 [TicketOrderServiceImpl.java#L158-L188](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L158-L188)：

- 只有 `orderStatus == 1` 的订单可以再变为 `2` 或 `3`；其他状态再来调用 `updateStatus` 会被直接抛错。
- "已核销 → 已取消"或"已取消 → 已核销"是**禁止**的。
- 删除接口 [delete()](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L141-L154) 是物理 `deleteById`，且**只允许在状态 3（已取消）时调用**。

---

## 3. 库存（stock）的变动点

只在两个地方会修改 `ticket_type.stock`：

### 3.1 下单时：扣库存（必扣）

入口：`POST /api/order/create` → [create()](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L36-L83)：

1. 校验票种存在、库存够。
2. 用 `version` 做乐观锁 `update`：`stock = stock - ticketNum`，同时 `version + 1`。见 [TicketOrderServiceImpl.java#L49-L60](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L49-L60)。
3. 更新行数为 0 时直接抛错回滚，避免超卖。
4. 库存扣完后再写订单，状态置 `1`。

> 这条链路是同步的：**库存是在下单瞬间被占用的，不存在"待支付/超时回滚"那一套**。

### 3.2 已下单 → 已取消：恢复库存

入口：`PUT /api/order/updateStatus?orderStatus=3` → [updateStatus()](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L156-L188)：

```
if (orderStatus == 3) { stock += order.ticketNum; }
```

具体见 [TicketOrderServiceImpl.java#L173-L183](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L173-L183)。

> 这里**没有用乐观锁**（没有比对 `version`，也没有 `version + 1`），仅 `eq id`，是与下单路径不一样的写法，并发取消时存在与票种编辑/下单互相覆盖的可能，属于潜在风险点。

### 3.3 不动库存的操作

| 操作 | 是否动库存 | 说明 |
| --- | --- | --- |
| 编辑订单（修改姓名/手机号） [update()](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L120-L138) | 否 | 仅覆盖 `userName / userPhone` |
| 已下单 → 已核销 | 否 | 库存早在下单时就扣了，核销不再变动 |
| 删除订单 [delete()](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L141-L154) | 否 | 只允许在"已取消"状态删除，此时库存已在取消时归还，所以删除不会再退库 |
| 票种 [delete()](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketTypeServiceImpl.java#L179-L195) | — | 一旦有任何关联订单就禁止删除，这是另一道闸 |

> ⚠️ **隐含坑**：删除"已取消"订单时库存不会再变。但因为这种订单的库存已在取消那一步退回，所以总账还是平的——前提是用户**严格按 1→3→delete** 的顺序走。如果将来允许直接删除 `orderStatus=1` 的订单，库存就会"凭空消失"。

---

## 4. 订单状态对"销量 / 销售额 / 页面展示"的影响

| 维度 | 看谁的状态 | 详细 |
| --- | --- | --- |
| 是否占库存 | 状态 `1`、`2` 都占（库存数值上已被扣走）；状态 `3` 不占（库存已归还） | — |
| 是否计入"销量 / 销售额" | **只算 `2 已核销`** | 见第 5 节 |
| 后台列表展示 | 全部状态都展示 | 仅按筛选条件过滤 |
| 后台可执行的操作 | 由 `orderStatus` 决定按钮显隐 | 见 [OrderManagement.vue#L54-L75](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-frontend/src/views/admin/OrderManagement.vue#L54-L75) |
| 游客购票页"剩余库存" | 直接读 `ticket_type.stock` | 见 [BuyTicket.vue#L53-L56](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-frontend/src/views/visitor/BuyTicket.vue#L53-L56)；下单成功后会刷新一次列表 [BuyTicket.vue#L262-L264](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-frontend/src/views/visitor/BuyTicket.vue#L262-L264) |

后台按钮规则（精简）：

- `orderStatus == 1`：显示"编辑 / 核销 / 取消"。
- `orderStatus == 2`：仅"编辑"（无法再回到 1，也不能取消，但"编辑"接口实际只改姓名/手机号，不会影响状态）。
- `orderStatus == 3`：显示"编辑 / 删除"。

---

## 5. 统计接口（销量、销售额、票种排行）

入口：`GET /api/order/statistics` → [statistics()](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L190-L240)。

筛选条件 ([TicketOrderServiceImpl.java#L192-L202](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L192-L202))：

```
createTime BETWEEN startTime AND endTime
AND orderStatus = 2   -- 只算已核销
```

聚合方式：

- `totalSales`：把符合条件订单的 `totalAmount` 全部累加（在内存里 stream 求和，**不在数据库聚合**）。
- `totalNum`：把 `ticketNum` 累加。
- `ticketTypeRank`：按 `ticketTypeId` 分组，对 `ticketNum` 求和，倒排前 10，再用 `selectById` 一票种一查地补 `typeName`（N+1 查询，量大时性能要注意）。

> 一句话：**"销量、销售额、票种排行"= 时间窗口内所有"已核销"订单的简单聚合**。
>
> 推论：
> - 状态 1（已下单未核销）和状态 3（已取消）都不进统计。
> - 极端情形下，库存被扣了（状态=1），但若一直没核销，统计页是 0；这就是"库存少了，销售额不动"的来源。
> - 删除"已取消"订单不会改变统计（本来就没在统计内）。

前端显示：

- 总销售额、总销量、平均单价、票种销量排行 → [Statistics.vue#L25-L65](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-frontend/src/views/admin/Statistics.vue#L25-L65)。
- 平均单价是前端用 `totalSales / totalNum` 临时算的，不来自后端。

---

## 6. 一笔订单的完整链路（含每一步的副作用）

下表把所有可能路径并到一起，便于审查"哪些动作改库存 / 销量 / 销售额 / 展示"。

| # | 操作 | 入口 | 库存 stock | 订单状态 | 销量·销售额（统计） | 后台列表 | 游客购票页 |
| - | ---- | ---- | --------- | -------- | ------------------- | -------- | ---------- |
| 1 | 下单 | `POST /api/order/create` | **− ticketNum**（乐观锁） | 新订单 = 1 | 不计入 | 新增一行（状态1） | 重新加载，剩余库存减少 |
| 2 | 编辑订单 | `PUT /api/order/update` | 不变 | 不变 | 不变 | 姓名/手机号更新 | 无影响 |
| 3 | 核销 | `PUT /api/order/updateStatus?orderStatus=2` | 不变 | 1 → 2 | **首次进入统计**，`totalSales += totalAmount`，`totalNum += ticketNum` | 状态变绿"已核销"，操作列只剩"编辑" | 无影响 |
| 4 | 取消 | `PUT /api/order/updateStatus?orderStatus=3` | **+ ticketNum**（无乐观锁） | 1 → 3 | 不变（本来也没进） | 状态变灰"已取消"，操作列出现"删除" | 库存增加（下次刷新可见） |
| 5 | 删除 | `DELETE /api/order/delete/{id}` | 不变 | 物理删除（只允许状态=3） | 不变 | 该行从列表消失 | 无影响 |

非法路径（会被服务端拒绝）：

- `2 → 任何` / `3 → 任何`：[updateStatus()](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L165-L171) 校验 `orderStatus != 1` 直接抛错。
- 删除非"已取消"订单：[delete()](file:///Users/lcz/AI/ai-apps-workspace/02.work%20session/session-gsb0608/gitlab%20source/label-213/label-213-summer/scenic-manager-backend/src/main/java/com/scenic/manager/service/impl/TicketOrderServiceImpl.java#L148-L151) 抛错。

---

## 7. 容易踩坑/容易看乱的几个点

1. **"销量"不是票种字段，是订单实时聚合**。改了订单状态就会立即影响下一次统计查询的结果，没有缓存。
2. **库存归还只发生在"1 → 3"那一次**。后续无论编辑还是删除都不会再动库存。
3. **统计仅看 `orderStatus = 2`**，所以"已下单但还没核销"的部分既占了库存又不在销售额里，是数据短期"对不上"的常见原因。
4. **`totalAmount` 是下单时刻的快照**。后续票种调价不会回写到历史订单，所以统计金额一定与"当前票价 × 销量"对不上，是预期行为。
5. **下单扣库存有乐观锁，取消归还库存没有乐观锁**。并发下取消 + 票种编辑可能互相覆盖。
6. **删除是物理删除**，不是软删，记录从此消失，统计也不会再看到。要做留痕需要另加字段或日志。
7. 后台列表 `list()` 不按状态过滤，是**全状态都展示**，统计页才是仅核销订单——这是两处经常被混为一谈的地方。

---

## 8. 一句话总结

> **库存是"下单时立即扣、取消时归还"，而销量/销售额是"只认已核销订单 + 时间窗口的实时聚合"。两条线相互独立，所以会出现"库存少了但销售额没动"或"销售额涨了但库存没再变"的现象——都是预期行为，不是 Bug。**
